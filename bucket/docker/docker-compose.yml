version: "3.8"

services:
  node:
    image: ${PROJECT}
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: ${PROJECT}
    ports:
      - ${PROJECT_PORT}:80
    volumes:
      - type: bind
        source: ../public
        target: /var/www/html
    tty: true
